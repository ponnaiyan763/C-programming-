#include<stdio.h>
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if (a>b){
        printf("%d is greater than",a);
    }
    else {
        printf("%d is greater",b);
    }
    return 0;
}